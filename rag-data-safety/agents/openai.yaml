interface:
  display_name: "RAG Data Safety"
  short_description: "Protect RAG data, memory, and trust boundaries"
  default_prompt: "Use $rag-data-safety to treat retrieved text as untrusted, require provenance, define quarantine and retention or deletion rules, and audit trust-boundary data flow."
