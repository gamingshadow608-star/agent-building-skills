## Verification Evidence

- Commands run:
  - `<command>` -> `pass` | `fail` | `blocked`
- Key logs or outputs:
  - `<important line or summary>`
- Startup verification:
  - Entry point: `<server|cli|worker|job>`
  - Command: `<command>`
  - Result: `pass` | `fail` | `blocked`
- Eval cases:
  - `<case-id>` -> `with_skill: pass|fail`, `without_skill: pass|fail`
- Blockers:
  - `<blocker, attempted command, and remaining unverified behavior>`
